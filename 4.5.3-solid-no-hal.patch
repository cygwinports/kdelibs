--- origsrc/kdelibs-4.5.3/solid/solid/CMakeLists.txt	2010-07-23 16:10:47.000000000 -0500
+++ src/kdelibs-4.5.3/solid/solid/CMakeLists.txt	2010-11-04 14:07:13.646427500 -0500
@@ -121,7 +121,7 @@ set(solid_LIB_SRCS
    backends/fakehw/fakesmartcardreader.cpp
 )
 
-if(NOT WIN32 AND NOT APPLE)
+if(NOT WIN32 AND NOT APPLE AND NOT CYGWIN)
 #   message(STATUS "-- Building Solid KUPnP backend." )
 #   set(solid_LIB_SRCS ${solid_LIB_SRCS}
 
@@ -167,7 +167,7 @@ if(NOT WIN32 AND NOT APPLE)
    backends/hal/halsmartcardreader.cpp
 
    )
-endif(NOT WIN32 AND NOT APPLE)
+endif(NOT WIN32 AND NOT APPLE AND NOT CYGWIN)
 
 if(APPLE)
    find_package(IOKit REQUIRED)
--- origsrc/kdelibs-4.5.3/solid/solid/managerbase.cpp	2010-07-23 16:10:47.000000000 -0500
+++ src/kdelibs-4.5.3/solid/solid/managerbase.cpp	2010-11-04 14:06:05.848709300 -0500
@@ -23,7 +23,8 @@
 
 #include "backends/fakehw/fakemanager.h"
 
-#if defined (Q_OS_MAC)
+#if defined (Q_OS_CYGWIN)
+#elif defined (Q_OS_MAC)
 #include "backends/iokit/iokitmanager.h"
 #elif defined (Q_OS_UNIX)
 #include "backends/hal/halmanager.h"
@@ -48,7 +49,8 @@ void Solid::ManagerBasePrivate::loadBack
     if (!solidFakeXml.isEmpty()) {
         m_backends << new Solid::Backends::Fake::FakeManager(0, solidFakeXml);
     } else {
-#        if defined (Q_OS_MAC)
+#        if defined (Q_OS_CYGWIN)
+#        elif defined (Q_OS_MAC)
             m_backends << new Solid::Backends::IOKit::IOKitManager(0);
 #        elif defined (Q_OS_UNIX)
             m_backends << new Solid::Backends::Hal::HalManager(0);
